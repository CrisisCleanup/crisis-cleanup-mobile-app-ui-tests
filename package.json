{
  "name": "crisis-cleanup-mobile-ui-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "args": "^5.0.3"
  }
}